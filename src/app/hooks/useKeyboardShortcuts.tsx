import { useEffect } from 'react';

/**
 * Global keyboard shortcuts hook
 * Currently a placeholder - shortcuts are implemented in individual components
 */
export function useGlobalKeyboardShortcuts() {
  useEffect(() => {
    // Keyboard shortcuts are implemented in App.tsx
    // This hook exists for future global shortcuts that need context access
  }, []);
}
