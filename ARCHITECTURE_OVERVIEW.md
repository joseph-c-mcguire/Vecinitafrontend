# Vecinita RAG Chatbot - Architecture Overview

This document provides a high-level overview of the system architecture and data flows.

**📚 For detailed implementation, see `/BACKEND_INTEGRATION_GUIDE.md`**

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
│                                                                  │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────────────────┐ │
│  │ Chat UI    │  │ Admin Panel  │  │ Settings & Controls     │ │
│  │            │  │              │  │ - Language Selector     │ │
│  │ - Messages │  │ - Documents  │  │ - Theme Toggle          │ │
│  │ - Input    │  │ - Upload     │  │ - Accessibility         │ │
│  │ - Sources  │  │ - View/Edit  │  │ - Backend Settings      │ │
│  └────────────┘  └──────────────┘  └─────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                 Context Providers                           │ │
│  │  Language | Accessibility | Auth | Backend Settings        │ │
│  └────────────────────────────────────────────────────────────┘ │
└───────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     SUPABASE CLIENT                              │
│                  (@supabase/supabase-js)                         │
│                                                                  │
│  - Authentication                                                │
│  - Database Queries (RLS-protected)                              │
│  - Real-time Subscriptions (optional)                            │
└───────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         BACKEND API                              │
│                    (Node.js / Edge Functions)                    │
│                                                                  │
│  ┌──────────────────┐              ┌──────────────────────────┐ │
│  │  Chat Endpoint   │              │  Document Processing     │ │
│  │  POST /api/chat  │              │  POST /api/documents/... │ │
│  │                  │              │                          │ │
│  │  1. Get query    │              │  1. Upload/scrape        │ │
│  │  2. Generate     │              │  2. Extract text         │ │
│  │     embedding    │              │  3. Chunk content        │ │
│  │  3. Vector search│              │  4. Generate embeddings  │ │
│  │  4. Build context│              │  5. Store in DB          │ │
│  │  5. Call LLM     │              │                          │ │
│  │  6. Return result│              │                          │ │
│  └──────────────────┘              └──────────────────────────┘ │
└───────────────────────────┬──────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
┌──────────────────┐ ┌──────────────┐ ┌─────────────────┐
│  LLM Providers   │ │  Embedding   │ │    Supabase     │
│                  │ │  Providers   │ │    Database     │
│ - OpenAI         │ │              │ │                 │
│ - Anthropic      │ │ - OpenAI     │ │ - PostgreSQL    │
│ - Groq           │ │ - Cohere     │ │ - pgvector      │
│ - DeepSeek       │ │ - HuggingFace│ │ - RLS Policies  │
│ - Ollama (local) │ │ - Voyage AI  │ │                 │
└──────────────────┘ └──────────────┘ └─────────────────┘
```

---

## 💬 Chat Message Flow (RAG)

```
User Input
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 1. Frontend - Chat Component                            │
│    - User types message                                 │
│    - Validates input                                    │
│    - Shows typing indicator                             │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 2. Frontend - Save to Supabase (if authenticated)       │
│    INSERT INTO chat_history (role='user', content=...)  │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Frontend - Call Backend API                          │
│    POST /api/chat                                       │
│    {                                                    │
│      message: "¿Qué es la calidad del agua?",          │
│      language: "es",                                    │
│      llm_settings: { provider, model },                 │
│      embedding_settings: { provider, model }            │
│    }                                                    │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Backend - Generate Query Embedding                   │
│    const embedding = await openai.embeddings.create({   │
│      model: "text-embedding-3-small",                   │
│      input: userMessage                                 │
│    });                                                  │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 5. Backend - Vector Search (Supabase RPC)               │
│    const { data: chunks } = await supabase.rpc(         │
│      'match_documents',                                 │
│      {                                                  │
│        query_embedding: embedding,                      │
│        match_threshold: 0.7,                            │
│        match_count: 5                                   │
│      }                                                  │
│    );                                                   │
│                                                         │
│    Returns top 5 most relevant document chunks          │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 6. Backend - Build Context Prompt                       │
│    const context = chunks.map(c => c.content).join()    │
│                                                         │
│    const prompt = `                                     │
│      Context: ${context}                                │
│      Question: ${userMessage}                           │
│      Answer in ${language}:                             │
│    `;                                                   │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 7. Backend - Call LLM                                   │
│    const completion = await openai.chat.completions     │
│      .create({                                          │
│        model: "gpt-4",                                  │
│        messages: [                                      │
│          { role: "system", content: systemPrompt },     │
│          { role: "user", content: prompt }              │
│        ]                                                │
│      });                                                │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 8. Backend - Format Response with Sources               │
│    return {                                             │
│      success: true,                                     │
│      data: {                                            │
│        response: completion.choices[0].message.content, │
│        sources: chunks.map(chunk => ({                  │
│          title: chunk.document_title,                   │
│          url: chunk.document_url,                       │
│          snippet: chunk.content.substring(0, 200),      │
│          relevance_score: chunk.similarity              │
│        }))                                              │
│      }                                                  │
│    };                                                   │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 9. Frontend - Display Response                          │
│    - Shows AI message with markdown formatting          │
│    - Displays source cards with links                   │
│    - Saves to chat_history (if authenticated)           │
└─────────────────────────────────────────────────────────┘
    │
    ▼
   Done!
```

---

## 📄 Document Upload & Processing Flow

```
Admin Uploads Document
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 1. Frontend - Admin Panel                               │
│    User selects:                                        │
│    - URL or File                                        │
│    - Document title                                     │
│    - Embedding settings                                 │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 2. Frontend - Validation                                │
│    - Check file type (PDF, DOCX, TXT, MD, HTML)         │
│    - Validate file size (max 10MB)                      │
│    - Validate URL format                                │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Frontend - Call Backend API                          │
│    POST /api/documents/process                          │
│    {                                                    │
│      source_type: "url" | "file",                       │
│      url: "...",  // or file in FormData                │
│      title: "Document Title",                           │
│      embedding_settings: { provider, model },           │
│      chunking_settings: { chunk_size, overlap }         │
│    }                                                    │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Backend - Return Job ID Immediately                  │
│    HTTP 202 Accepted                                    │
│    { job_id: "uuid", status: "processing" }             │
│                                                         │
│    Process continues asynchronously...                  │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 5. Backend Async - Extract Content                      │
│    IF URL:                                              │
│      - Fetch webpage with axios/fetch                   │
│      - Parse HTML with cheerio                          │
│      - Extract main content                             │
│    IF FILE:                                             │
│      - PDF: pdf-parse                                   │
│      - DOCX: mammoth                                    │
│      - TXT/MD: direct read                              │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 6. Backend - Chunk Text                                 │
│    const chunks = [];                                   │
│    const chunkSize = 1000;                              │
│    const overlap = 200;                                 │
│                                                         │
│    for (let i = 0; i < text.length; i += chunkSize - overlap) {│
│      chunks.push(text.slice(i, i + chunkSize));         │
│    }                                                    │
│                                                         │
│    Result: ~47 chunks from typical article              │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 7. Backend - Generate Embeddings (Batch)                │
│    const embeddings = await Promise.all(                │
│      chunks.map(chunk =>                                │
│        openai.embeddings.create({                       │
│          model: "text-embedding-3-small",               │
│          input: chunk                                   │
│        })                                               │
│      )                                                  │
│    );                                                   │
│                                                         │
│    Each embedding = 1536-dimensional vector             │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 8. Backend - Store in Supabase                          │
│                                                         │
│    A. Create document record:                           │
│       INSERT INTO vector_documents (                    │
│         title, source_url, total_chunks                 │
│       )                                                 │
│                                                         │
│    B. Store chunks with embeddings:                     │
│       INSERT INTO document_chunks (                     │
│         document_id, chunk_index, content, embedding    │
│       )                                                 │
│                                                         │
│    C. Update job status:                                │
│       status = "completed"                              │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 9. Frontend - Poll for Status (every 2 seconds)         │
│    GET /api/documents/status/{job_id}                   │
│                                                         │
│    Shows progress:                                      │
│    "Extracting content..." → 25%                        │
│    "Chunking text..." → 50%                             │
│    "Generating embeddings..." → 75%                     │
│    "Storing in database..." → 90%                       │
│    "Complete!" → 100%                                   │
└─────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 10. Frontend - Refresh Document List                    │
│     Calls Admin Dashboard refresh()                     │
│     Shows new document in table                         │
└─────────────────────────────────────────────────────────┘
    │
    ▼
   Done! Document ready for RAG queries
```

---

## 🗄️ Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                     auth.users (Supabase)                    │
│  ┌────────┬──────────┬──────────────────┐                   │
│  │ id     │ email    │ created_at       │                   │
│  └────────┴──────────┴──────────────────┘                   │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ user_id (FK)
                            │
            ┌───────────────┼────────────────┐
            ▼               ▼                ▼
┌────────────────────┐ ┌─────────────┐ ┌──────────────┐
│  chat_sessions     │ │ admin_users │ │ chat_history │
│                    │ │             │ │              │
│ ┌────────────────┐ │ │ ┌─────────┐ │ │  (orphaned   │
│ │ id (PK)        │ │ │ │ id (PK) │ │ │   if user    │
│ │ user_id (FK)   │ │ │ │ email   │ │ │   deletes    │
│ │ title          │ │ │ └─────────┘ │ │   session)   │
│ │ created_at     │ │ └─────────────┘ │              │
│ │ updated_at     │ │                 │              │
│ └────────────────┘ │                 └──────────────┘
└──────┬─────────────┘
       │
       │ session_id (FK)
       │
       ▼
┌────────────────────────────────┐
│      chat_history              │
│                                │
│ ┌────────────────────────────┐ │
│ │ id (PK)                    │ │
│ │ session_id (FK)            │ │
│ │ role (user|assistant)      │ │
│ │ content                    │ │
│ │ sources (JSONB)            │ │
│ │ llm_model                  │ │
│ │ created_at                 │ │
│ └────────────────────────────┘ │
└────────────────────────────────┘


┌────────────────────────────────────────────────────────┐
│              vector_documents                          │
│                                                        │
│ ┌────────────────────────────────────────────────────┐ │
│ │ id (PK)                                            │ │
│ │ title                                              │ │
│ │ source_url                                         │ │
│ │ content_type                                       │ │
│ │ total_chunks                                       │ │
│ │ metadata (JSONB)                                   │ │
│ │ created_at                                         │ │
│ │ updated_at                                         │ │
│ └────────────────────────────────────────────────────┘ │
└──────────────────┬─────────────────────────────────────┘
                   │
                   │ document_id (FK)
                   │ CASCADE DELETE
                   │
                   ▼
┌────────────────────────────────────────────────────────┐
│              document_chunks                           │
│                                                        │
│ ┌────────────────────────────────────────────────────┐ │
│ │ id (PK)                                            │ │
│ │ document_id (FK) → vector_documents.id             │ │
│ │ chunk_index                                        │ │
│ │ content (TEXT)                                     │ │
│ │ embedding (vector(1536)) ← INDEXED WITH IVFFLAT   │ │
│ │ token_count                                        │ │
│ │ metadata (JSONB)                                   │ │
│ │ created_at                                         │ │
│ └────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────┘
                   │
                   │ Used by RPC function
                   │
                   ▼
            match_documents()
      (Cosine similarity search)
```

---

## 🔒 Security & Authentication Flow

```
┌─────────────────────────────────────────────────────────┐
│                 FRONTEND APPLICATION                     │
└─────────────────────────────────────────────────────────┘
                   │
                   │ User actions
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
┌─────────────┐ ┌──────────┐ ┌────────────────┐
│  Anonymous  │ │  Normal  │ │     Admin      │
│    User     │ │   User   │ │     User       │
└─────────────┘ └──────────┘ └────────────────┘
      │              │              │
      │              │              │
      ▼              ▼              ▼
┌─────────────────────────────────────────────────┐
│             Can Access                          │
├─────────────────────────────────────────────────┤
│ Anonymous: - Chat interface (no history save)   │
│            - View responses                     │
│            - Change settings                    │
│                                                 │
│ User:      - All anonymous features             │
│            - Save chat history                  │
│            - Load previous sessions             │
│            - Persist across devices             │
│                                                 │
│ Admin:     - All user features                  │
│            - Admin panel access                 │
│            - Upload documents                   │
│            - Delete documents                   │
│            - View vector database               │
└─────────────────────────────────────────────────┘

Authentication Methods:

1. Supabase Auth (Normal Users)
   ┌──────────────────────────────────────┐
   │ - Email + Password                   │
   │ - Magic Link                         │
   │ - OAuth (Google, GitHub, etc.)       │
   │                                      │
   │ Returns: JWT access_token            │
   │ Stored in: localStorage              │
   │ Expires: 1 hour (auto-refresh)       │
   └──────────────────────────────────────┘

2. Token-based Auth (Admin)
   ┌──────────────────────────────────────┐
   │ 1. Admin enters token in login form  │
   │ 2. Frontend hashes with SHA-256      │
   │ 3. Compares with VITE_ADMIN_TOKEN_HASH│
   │ 4. If match, sets sessionStorage     │
   │                                      │
   │ Stored in: sessionStorage only       │
   │ Expires: When browser tab closes     │
   └──────────────────────────────────────┘

Row Level Security (RLS) Policies:

┌────────────────────────────────────────────────┐
│  chat_sessions                                 │
│  ✓ Users can only see their own sessions      │
│  ✓ Users can only insert with their user_id   │
│  ✓ Users can update their own sessions        │
│  ✓ Users can delete their own sessions        │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│  chat_history                                  │
│  ✓ Users can view messages from their sessions│
│  ✓ Users can insert to their sessions         │
│  ✗ Users cannot update/delete messages        │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│  vector_documents & document_chunks            │
│  ✓ All users can read (public knowledge base) │
│  ✗ Only admins can write (via backend API)    │
└────────────────────────────────────────────────┘
```

---

## ⚡ Performance Optimizations

### 1. Vector Search Index

```sql
-- IVFFlat index for fast approximate search
CREATE INDEX idx_document_chunks_embedding 
  ON document_chunks 
  USING ivfflat (embedding vector_cosine_ops)
  WITH (lists = 100);

-- Trade-off:
-- - Faster queries (10-100x)
-- - Slightly less accurate than exact search
-- - Good for 1M+ vectors
```

### 2. Caching Strategy

```
┌─────────────────────────────────────────┐
│  Query Caching (Future Enhancement)     │
│                                         │
│  Cache Key: hash(query + language)      │
│  Cache Value: {response, sources}       │
│  TTL: 1 hour                            │
│                                         │
│  Benefits:                              │
│  - Instant responses for common queries │
│  - Reduced LLM API costs                │
│  - Lower latency                        │
└─────────────────────────────────────────┘
```

### 3. Batch Processing

```
Document Upload: Process chunks in parallel

Sequential (slow):
  Extract → Chunk 1 → Embed 1 → Store 1
         → Chunk 2 → Embed 2 → Store 2
         → Chunk 3 → Embed 3 → Store 3
  Total: ~30 seconds

Parallel (fast):
  Extract → [Chunk 1, Chunk 2, Chunk 3]
         → [Embed 1, Embed 2, Embed 3] (parallel)
         → [Store all] (batch insert)
  Total: ~8 seconds
```

---

## 🌍 Internationalization (i18n) Flow

```
┌────────────────────────────────────────────────┐
│  Language Context (React Context API)          │
│                                                │
│  State: { language: "es" | "en" }              │
│  Persisted: localStorage.getItem('language')   │
│  Default: "es" (Spanish)                       │
└────────────────────────────────────────────────┘
         │
         │ Provides translations
         │
         ▼
┌────────────────────────────────────────────────┐
│  All Components use t() function               │
│                                                │
│  Example:                                      │
│  const { t, language } = useLanguage();        │
│  <h1>{t('welcome')}</h1>                       │
│                                                │
│  Returns:                                      │
│  ES: "Bienvenido a Vecinita"                   │
│  EN: "Welcome to Vecinita"                     │
└────────────────────────────────────────────────┘
         │
         │ Sends to backend
         │
         ▼
┌────────────────────────────────────────────────┐
│  Backend uses language parameter               │
│                                                │
│  POST /api/chat { language: "es", ... }        │
│                                                │
│  - Retrieves docs in correct language          │
│  - Instructs LLM to respond in language        │
│  - Returns translated error messages           │
└────────────────────────────────────────────────┘
```

---

## 📱 Responsive Design Breakpoints

```
┌──────────────────────────────────────────────┐
│  Mobile First Approach                        │
└──────────────────────────────────────────────┘

< 640px (Mobile)
  - Single column layout
  - Collapsed sidebar (overlay when open)
  - Smaller fonts
  - Hide admin/settings buttons (keyboard only)
  - Stack sources vertically

640px - 768px (Tablet)
  - Slightly larger fonts
  - Still collapsed sidebar
  - More padding

768px+ (Desktop)
  - Two column layout (sidebar + chat)
  - Larger fonts and spacing
  - Show all navigation buttons
  - Sources in grid layout
  - Admin table view
```

---

## 🎨 Theme System

```
┌────────────────────────────────────────────┐
│  CSS Variables (Tailwind v4)               │
│  /src/styles/theme.css                     │
└────────────────────────────────────────────┘

Light Theme:
  --color-background: white
  --color-foreground: black
  --color-primary: #4DB8B8 (turquoise)
  --color-border: #e5e7eb
  ...

Dark Theme (.dark class):
  --color-background: #1a1a1a
  --color-foreground: #ffffff
  --color-primary: #4DB8B8 (same)
  --color-border: #374151
  ...

Toggle:
  document.documentElement.classList.toggle('dark')
  localStorage.setItem('theme', 'dark'|'light')
```

---

## 📊 Monitoring & Observability (Future)

```
Recommended Metrics to Track:

Frontend:
  - Page load time
  - Time to first byte (TTFB)
  - Chat response latency
  - Error rates by component

Backend:
  - API response times
  - LLM API latency
  - Embedding generation time
  - Vector search performance
  - Database query times
  - Error rates by endpoint

Costs:
  - LLM API usage (tokens/requests)
  - Embedding API usage
  - Supabase database size
  - Bandwidth usage

Tools:
  - Supabase Dashboard (built-in metrics)
  - Sentry (error tracking)
  - LogRocket (session replay)
  - Vercel Analytics (if using Vercel)
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────┐
│                   PRODUCTION                         │
└─────────────────────────────────────────────────────┘

Frontend (Static):
  ┌──────────────────────────────────┐
  │  Vercel / Netlify / Cloudflare   │
  │  - Build: npm run build          │
  │  - Output: /dist                 │
  │  - CDN: Global edge network      │
  │  - SSL: Automatic                │
  └──────────────────────────────────┘

Backend (Serverless):
  ┌──────────────────────────────────┐
  │  Supabase Edge Functions         │
  │  - Runtime: Deno                 │
  │  - Deploy: supabase functions    │
  │  - Auto-scaling                  │
  │  - Global distribution           │
  └──────────────────────────────────┘
          ↓
  ┌──────────────────────────────────┐
  │  Supabase (Database + Auth)      │
  │  - PostgreSQL 15                 │
  │  - pgvector extension            │
  │  - Automatic backups             │
  │  - Connection pooling            │
  └──────────────────────────────────┘

External APIs:
  ┌──────────────────────────────────┐
  │  LLM Providers                   │
  │  - OpenAI / Anthropic / Groq     │
  │  - Rate limiting                 │
  │  - Fallback providers            │
  └──────────────────────────────────┘
```

---

## 📝 Next Steps

1. **Review** this architecture document
2. **Set up** Supabase database (see `/INTEGRATION_QUICK_START.md`)
3. **Implement** backend endpoints (see `/BACKEND_API_SPECIFICATION.md`)
4. **Test** each integration independently
5. **Deploy** to staging environment
6. **Optimize** based on real-world usage
7. **Monitor** and iterate

For detailed implementation steps, see `/BACKEND_IMPLEMENTATION.md`.